import React from 'react';
import SignupContainer from '../containers/signup/signupContainer';

const SignupScene = (props) => (
  <SignupContainer navigation={props.navigation}/>
)

export default SignupScene;
