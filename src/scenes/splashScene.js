import React from 'react';
import SplashContainer from '../containers/splash/splashContainer';

const SplashScene = (props) => (
  <SplashContainer navigation={props.navigation}/>
)

export default SplashScene;
